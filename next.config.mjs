/** @type {import('next').NextConfig} */

// Hosted on Vercel — regular server build with API routes.
// (The old GitHub Pages static-export config — output: "export", basePath,
// assetPrefix — is gone; assets and links now resolve from the site root.)
const nextConfig = {};

export default nextConfig;
